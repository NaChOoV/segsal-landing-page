import { lazy } from 'react';

export const routes = {
  areasIndex: '/areas',
  areaSubareas: '/areas/:areaSlug',
  subareaDetail: '/areas/:areaSlug/:subareaSlug',
};

export default function AppRouter() {
  return null; // TODO: integrar React Router
}
